import React from "react";

const AuthComponent = () => (
  <React.Fragment>
    <div className="filter">Login/Register</div>
    <div className="main">Login / Register forms</div>
  </React.Fragment>
);

export default AuthComponent;
